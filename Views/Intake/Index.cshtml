@*@model IEnumerable<Intake>

<p>
    <a asp-action="Create" class="btn btn-primary">Add Intake</a>
</p>


<table class="table">
<thead>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StartDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EndDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Duration)
        </th>
        <th>
            Intake Creation Date
        </th>
        <th>
            Admin Name
        </th>
    </tr>
</thead>
<tbody>
    @foreach (var intake in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => intake.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => intake.StartDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => intake.EndDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => intake.Duration)
            </td>
            <td>
                @Html.DisplayFor(modelItem => intake.CreationDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => intake.Admin.FullName)
            </td>
            <td>
                <a asp-controller="Intake" asp-action="Edit" asp-route-id="@intake.ID" class="btn btn-warning">Edit</a>
            </td>
            <td>
               <a asp-action="Delete" asp-controller="Intake" asp-route-id="@intake.ID" class="btn btn-danger">Delete</a>
            </td>  
        </tr>
    }
</tbody>
</table>*@